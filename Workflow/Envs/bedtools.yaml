name: bedtools_env
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bedtools=2.30.0
  - python=3.9
  - pybedtools
  - pandas
  - pip
  - pip:
      - cython
      - pybedtools
